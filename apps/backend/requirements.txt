# Pinned to versions known to work together on Python 3.11+.
# Server runs on 3.9+ for local dev.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0
pydantic>=2.5,<3.0
stripe>=7,<16
PyJWT[crypto]>=2.8,<3.0
webauthn>=2.0,<3.0
python-multipart>=0.0.9,<1.0
# Apple's supported App Store Server Library — signed-data (JWS) verification
# against Apple's root chain (X509_STRICT + marker OIDs) and the App Store
# Server API (current-provider status + Set-App-Account-Token). Replaces the
# home-grown PKI a prior build-91 candidate shipped (contract §3).
app-store-server-library>=1.2,<2.0

# Google Play Developer API (androidpublisher v3) server-side subscription
# verification + acknowledgement, and OIDC verification of Real-time Developer
# Notification (RTDN) Pub/Sub push id-tokens. Server credentials arrive only via
# environment (TONO_GOOGLE_SERVICE_ACCOUNT_JSON); no secret reaches a client.
google-api-python-client>=2.100,<3.0
google-auth>=2.23,<3.0

# Tests
pytest>=7,<9
pytest-asyncio>=0.23,<2
