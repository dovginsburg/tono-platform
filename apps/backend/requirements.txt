# Pinned to versions known to work together on Python 3.11+.
# Server runs on 3.9+ for local dev.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0
pydantic>=2.5,<3.0
stripe>=7,<16
PyJWT[crypto]>=2.8,<3.0
webauthn>=2.0,<3.0

# Persistence: Postgres via SQLAlchemy's async engine + the asyncpg driver.
# Redis backs short-lived, single-use state (WebAuthn challenges, Slack/IP
# rate-limit windows) that used to live in a module-level dict — only
# correct with exactly one worker process.
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
redis>=5.0,<6.0
alembic>=1.13,<2.0

# Tests
pytest>=7,<9
pytest-asyncio>=0.23,<2
