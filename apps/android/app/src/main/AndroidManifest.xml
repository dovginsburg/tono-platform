<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Required on Android 13+ to post notifications (weekly digest) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".TonoApplication"
        android:label="Tono"
        android:theme="@style/Theme.Tono"
        android:allowBackup="false">

        <!-- Host app activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- IME service — the keyboard. BIND_INPUT_METHOD is required by Android.
             Users enable it in Settings → System → Languages & Input → On-screen keyboard. -->
        <service
            android:name="com.tono.ime.TonoImeService"
            android:label="Tono"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- WorkManager provider is auto-registered by the library; no explicit entry needed -->

    </application>
</manifest>
